<p id="notice"><%= notice %></p>

<h1>Images Uploadé Sur Le Site</h1>

<table>
  <thead>
    <tr>
      <th>-</th>
      <th>Name</th>
      <th>-</th>
      <th>Utilisateur</th>
      <th>-</th>
      <th>Valider</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @images.each do |image| %>
      <tr>
        <td>-</td>
        <td><%= image.name %></td>
        <td>-</td>
        <td><%= image.utilisateur_id %></td>
        <td>-</td>
        <td><%= image.valider %></td>
        <td><%= link_to 'Show', image %></td>
        <td><%= link_to 'Edit', edit_image_path(image) %></td>
        <td><%= link_to 'Destroy', image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvelle Image', new_image_path %> <%= link_to 'Editer l\'Utilisateur', edit_utilisateur_registration_path %>
